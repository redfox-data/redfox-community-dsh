---
name: xiaohongshu-cover
description: 专为小红书内容创作打造的AI封面设计工具，基于全网每日持续收录的2000+条爆款笔记数据，深度抓取同赛道爆款封面的视觉元素，通过AI智能分析总结高转化视觉规律，生成贴合笔记内容、符合平台流量审美的可落地封面设计方案，省去繁琐设计流程，提升笔记封面点击率。
dependency:
  python:
    - requests>=2.28.0
  system:
---

# 小红书爆款封面生成

## 简介

小红书爆款封面生成是一款专为小红书内容创作者打造的 AI 封面设计工具，基于全网每日持续收录的 **2000+ 条爆款笔记数据**，深度抓取同赛道爆款封面的视觉元素，通过 AI 智能分析总结高转化视觉规律，生成贴合笔记内容、符合平台流量审美的可落地封面设计方案。

通过简单的主题描述，你可以：
- 🔍 自动解析创作意图，精准提取关键信息
- 📊 获取目标领域真实爆款封面图，AI 分析总结风格特征
- 🎨 结合用户需求与爆款数据，生成可落地的设计方案（含案例 + 生图提示词）

适用于小红书博主、内容运营、品牌方、MCN 机构等需要提升笔记封面点击率的场景。

---

## 功能特性

### 🎯 核心功能
- **🧠 智能意图解析**：自动解析用户文案意图，提取内容主题、类型、风格偏好和核心关键词
- **📊 爆款数据查询**：调用红狐数据接口，获取低粉高赞、点赞最多、单日互动爆发、7日持续增长四维爆款数据
- **🖼️ AI 图像分析**：逐张识别爆款封面图的文字、场景、色调、构图等视觉元素，分类汇总风格规律
- **🎨 定制方案生成**：结合用户需求与爆款风格特征，输出三种差异化封面设计方案（含生图提示词）

### ✨ 特色亮点
- **📈 数据驱动**：基于日更 2000+ 条真实爆款笔记数据，拒绝拍脑袋设计
- **⚡ 全流程自动化**：从意图解析到方案生成，7 步标准流程一键完成
- **🎯 精准匹配**：智能评分筛选机制，确保推荐内容与用户需求高度相关
- **📱 平台适配**：所有方案均适配小红书 3:4 竖版封面规格（1080x1440像素）

---

## ⚠️ 触发规则

**以下情况才执行任务流程**：
- 用户提供了具体的内容主题或赛道（如"帮我设计美妆教程封面"、"职场穿搭封面怎么设计"）
- 用户明确要求生成封面方案
- 用户上传了文案或图片要求分析

**以下情况只介绍技能，不执行任务**：
- 用户询问"这个技能能做什么"、"怎么用"
- 用户只是了解功能，未提出具体需求
- 用户未提供明确的创作主题或方向

---

## ⚠️ 术语规范（强制执行）

**本技能统一使用"爆款封面"这一术语，禁止使用以下词汇**：

| ❌ 禁止使用 | ✅ 正确表述 |
|------------|------------|
| 爆炸封面 | 爆款封面 |
| 爆炸数据 | 爆款数据 |
| 抓取数据 | 获取/查询数据 |
| 抓取封面 | 获取封面 |

**⚠️ 强制要求**：在执行过程中，所有对外输出的文字必须使用"爆款封面"相关表述，包括但不限于：
- 标题、段落、步骤说明
- 对话回复
- 生成的分析报告
- 生成的提示词

---

## 一键安装

### 前置条件
- Python 3.8+ 运行环境
- 已获取红狐数据 API Key

### 获取 API Key
1. 访问 [红狐Hub 官网](https://redfox.hk/?source=github) 了解服务详情
2. 前往 [注册页面](https://redfox.hk/login?source=github) 注册账号
3. **新注册用户将获赠免费积分**，可立即开始使用 API 服务
4. 注册登录后，在个人中心获取 API Key，格式为 `ak_xxxxxxxx`

### 配置 API Key
`REDFOX_API_KEY` 从环境变量获取，格式 `ak_xxxxxxxx`

若未设置，提示用户自行配置；若用户不会配置，Agent 应主动帮用户设置：

| 操作系统 | 配置命令 |
|----------|----------|
| macOS/Linux（zsh） | 将 `export REDFOX_API_KEY=<值>` 追加到 `~/.zshrc`，然后 `source ~/.zshrc` |
| macOS/Linux（bash） | 将 `export REDFOX_API_KEY=<值>` 追加到 `~/.bashrc`，然后 `source ~/.bashrc` |
| Windows | 执行 `[Environment]::SetEnvironmentVariable("REDFOX_API_KEY", "<值>", "User")`（需重启终端生效） |

配置完成后应验证：`echo $REDFOX_API_KEY`（macOS/Linux）或 `echo %REDFOX_API_KEY%`（Windows），确保换一个 skill 也能读取到

### 依赖安装
```
pip install requests>=2.28.0
```

---

## 使用指南

### 基础使用

#### 1. 描述你的封面需求
告诉助手你想设计的封面主题：

> 用户：帮我设计一个穿搭类封面
>
> 助手：好的！我将为您分析穿搭赛道的爆款封面数据，生成专属封面设计方案。

#### 2. 等待分析完成
助手将自动执行7步工作流程（意图解析 → 数据查询 → 智能筛选 → 图像分析 → 风格总结 → 方案生成 → 自检），全程约1-2分钟。

#### 3. 选择喜欢的方案
查看三种不同风格的封面方案，获取生图提示词，直接用于封面制作。

### 高级使用

#### 自定义时间范围
> 用户：帮我分析近7天的穿搭爆款封面

#### 指定细分赛道
> 用户：设计一个小个子通勤穿搭的封面，风格偏韩系

#### 上传图片结合分析
> 用户：[上传图片] 结合我的照片和爆款方案生成专属封面

### 完整工作流程

核心工作流程包含 **7个标准步骤**，详细执行规范请参考 [references/core_workflow.md](references/core_workflow.md)：

| 步骤 | 名称 | 说明 |
|------|------|------|
| 步骤1 | 用户意图解析 | 结构化解析需求，提取主题/类型/风格/关键词 |
| 步骤2 | 爆款数据查询 | 调用脚本获取四维爆款数据 |
| 步骤3 | 智能匹配筛选 | 综合评分筛选20条最相关数据 |
| 步骤4 | AI图像分析 | 逐张识别封面视觉元素，分类汇总 |
| 步骤5 | 风格总结 | 输出爆款封面风格总结表（含真实图片） |
| 步骤6 | 方案生成 | 输出三种差异化封面设计方案 |
| 步骤7 | 输出自检 | 逐项检查输出完整性 |

> ⚠️ **重要**：执行任务时，Agent 必须严格按照 [core_workflow.md](references/core_workflow.md) 中定义的流程逐步执行，不可跳过任何步骤。

---

## 使用场景

### 场景一：博主封面设计
**角色**：小红书穿搭博主

**需求**：为每周的穿搭合集笔记设计高点击率封面

**使用方式**：
1. 描述穿搭赛道和风格偏好
2. 获取同赛道爆款封面风格分析
3. 选择合适的方案，使用生图提示词制作封面

**预期收益**：省去繁琐设计流程，封面点击率显著提升

---

### 场景二：品牌方内容营销
**角色**：品牌内容运营

**需求**：为品牌种草笔记设计符合平台调性的封面

**使用方式**：
1. 输入品牌产品关键词和目标受众
2. 分析竞品爆款封面的视觉规律
3. 结合品牌调性生成定制封面方案

**预期收益**：品牌内容更贴合平台审美，提升种草转化率

---

### 场景三：MCN 达人矩阵运营
**角色**：MCN 运营经理

**需求**：为旗下多个达人批量产出差异化封面方案

**使用方式**：
1. 按达人赛道分别查询爆款数据
2. 为每位达人匹配不同风格的封面方案
3. 方案间保持差异化，避免同质化

**预期收益**：批量高效产出封面，矩阵账号内容差异化

---

### 场景四：冷启动账号破零
**角色**：新人小红书博主

**需求**：账号刚起步，不知道如何设计吸引人的封面

**使用方式**：
1. 描述自己的内容方向和目标受众
2. 参考低粉高赞博主的成功封面案例
3. 使用生图提示词快速制作专业封面

**预期收益**：降低设计门槛，新账号也能产出专业封面

---

## 项目架构

### 目录结构

```
xiaohongshu-cover/
├── SKILL.md                        # 技能主文档（入口）
├── references/
│   ├── core_workflow.md             # 核心工作流程（7步执行规范）
│   └── xhs_trend_data_format.md    # 数据格式说明
├── scripts/
│   └── fetch_explosive_covers.py    # 爆款数据查询脚本
```

### 技术栈

| 组件 | 技术说明 |
|------|----------|
| 运行环境 | Python 3.8+ |
| 数据源 | 红狐数据 API（redfox.hk） |
| HTTP 请求 | requests >= 2.28.0 |
| 图像分析 | Agent 原生图片理解能力 |
| 数据格式 | JSON 输入/输出 |

### 核心模块说明

| 模块 | 职责 |
|------|------|
| `SKILL.md` | 技能入口，定义简介、功能、安装、使用方式，引用工作流程 |
| `references/core_workflow.md` | 定义7步标准执行流程，含评分公式、输出模板、自检清单 |
| `references/xhs_trend_data_format.md` | 定义 API 返回数据格式规范，含字段说明和示例 |
| `scripts/fetch_explosive_covers.py` | 调用红狐数据接口，获取四维爆款数据，支持多关键词查询 |


---

## 常见问答

### 安装相关问题

**Q1: 提示"未找到 REDFOX_API_KEY 环境变量"怎么办？**

A: 请按以下步骤配置：
1. 访问 [红狐Hub](https://redfox.hk?source=github) 注册账号并获取 API Key
2. Windows 用户：在 PowerShell 中执行 `[Environment]::SetEnvironmentVariable("REDFOX_API_KEY", "你的API Key", "User")`，然后重启终端
3. macOS/Linux 用户：将 `export REDFOX_API_KEY=你的API Key` 追加到 shell 配置文件，然后 `source` 使其生效
4. 验证配置：执行 `echo %REDFOX_API_KEY%`（Windows）或 `echo $REDFOX_API_KEY`（macOS/Linux）

**Q2: 支持哪些平台部署？**

A: 本技能为 Agent Skill 格式，支持所有兼容 SKILL.md 规范的 AI Agent 平台。

---

### 使用相关问题

**Q3: 查询的数据时间范围是多久？**

A: 数据库仅包含昨天至30天前的数据，默认查询近30天。如需自定义时间范围，可在请求时指定"近N天"。

**Q4: 可以同时查询多个关键词吗？**

A: 支持。最多5个关键词，用逗号分隔（如：`职场穿搭,通勤穿搭,面试穿搭`），总长度不超过200字符。

**Q5: 生成的封面方案可以直接使用吗？**

A: 每个方案均包含生图提示词，可直接用于 AI 绘图工具生成封面图。提示词已包含 3:4 竖版比例（1080x1440像素）要求。

---

### 故障排除

**Q6: API 调用失败怎么办？**

A: 请按以下步骤排查：
1. 确认 API Key 是否正确配置（参考 Q1）
2. 确认网络连接正常
3. 脚本内置3次自动重试，如仍失败请稍后再试
4. 确认 API Key 是否仍有余额

**Q7: 返回数据为空怎么办？**

A: 可能原因：
- 关键词过于小众或新兴，爆款内容积累较少
- 近期该赛道热度较低
- 建议更换更热门的关键词，如"早八穿搭"、"减脂餐"等

**Q8: 笔记链接或作者链接无法跳转怎么办？**

A: 可能原因及解决方案：
1. **作者链接 userId 缺失**：若作者链接显示为 `https://www.xiaohongshu.com/user/profile/`（末尾无 ID），说明数据提取时遗漏了 userId 字段，属于执行流程问题，请重新运行分析
2. **小红书风控拦截**：小红书对第三方跳转链接有风控策略，即使链接格式正确也可能无法直接跳转。建议复制作品标题到小红书 App 内搜索查看

---
